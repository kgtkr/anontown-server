import type   { TopicNormalResolvers } from './../../types.generated';
    export const TopicNormal: TopicNormalResolvers = {
    /* Implement TopicNormal resolver logic here */
        subscribe: () => { /* TopicNormal.subscribe resolver is required because TopicNormal.subscribe exists but TopicNormalMapper.subscribe does not */ }
    };
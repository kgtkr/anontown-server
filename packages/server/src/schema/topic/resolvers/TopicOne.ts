import type   { TopicOneResolvers } from './../../types.generated';
    export const TopicOne: TopicOneResolvers = {
    /* Implement TopicOne resolver logic here */
        subscribe: () => { /* TopicOne.subscribe resolver is required because TopicOne.subscribe exists but TopicOneMapper.subscribe does not */ }
    };
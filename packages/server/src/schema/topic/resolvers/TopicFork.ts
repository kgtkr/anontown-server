import type   { TopicForkResolvers } from './../../types.generated';
    export const TopicFork: TopicForkResolvers = {
    /* Implement TopicFork resolver logic here */
        active: () => { /* TopicFork.active resolver is required because TopicFork.active exists but TopicForkMapper.active does not */ },
        date: () => { /* TopicFork.date resolver is required because TopicFork.date exists but TopicForkMapper.date does not */ },
        id: () => { /* TopicFork.id resolver is required because TopicFork.id exists but TopicForkMapper.id does not */ },
        resCount: () => { /* TopicFork.resCount resolver is required because TopicFork.resCount exists but TopicForkMapper.resCount does not */ },
        subscribe: () => { /* TopicFork.subscribe resolver is required because TopicFork.subscribe exists but TopicForkMapper.subscribe does not */ },
        title: () => { /* TopicFork.title resolver is required because TopicFork.title exists but TopicForkMapper.title does not */ },
        update: () => { /* TopicFork.update resolver is required because TopicFork.update exists but TopicForkMapper.update does not */ },
        parent: () => { /* TopicFork.parent resolver is required because TopicFork.parent exists but TopicForkMapper.parent does not */ }
    };